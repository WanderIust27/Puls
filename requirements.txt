fastapi>=0.115
uvicorn[standard]>=0.30
httpx>=0.27
garminconnect>=0.3.2
garmin-fit-sdk>=21.0
fit-tool>=0.9.13
apscheduler>=3.10,<4
python-multipart>=0.0.9

# Wissensdatenbank. sqlite-vec braucht ein SQLite mit Erweiterungs-Unterstuetzung
# — python:3.12-slim kann das, Alpine nicht.
sqlite-vec>=0.1.6
requests>=2.31
# Einbettungen auf der CPU, damit Ollama die Karte fuer das Chat-Modell behaelt.
# fastembed ist ONNX und braucht kein PyTorch: rund 250 MB statt 2 GB Image.
# Wer stattdessen e5-small will (PULS_EMBED=e5), installiert zusaetzlich
# sentence-transformers — das zieht PyTorch nach.
fastembed>=0.3
